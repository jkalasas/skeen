// place files you want to import through the `$lib` alias in this folder.

// Platform detection utilities for Tauri/Web hybrid app
export { isTauri, isWeb, getPlatform, platform, openUrl } from './platform';
